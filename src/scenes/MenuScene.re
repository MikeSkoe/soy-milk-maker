let mouseDown = (state, env) =>
  state

let update = (state, env) => 
  state

let draw = (state, env) =>
  state
    |> Stats.draw(_, env)

let keyPressed = (state, env) => 
  state
    |> Stats.keyPressed(_, env);
